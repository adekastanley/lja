import { cn } from "@/lib/utils";
import { h5 } from "motion/react-client";

export const Logo = ({
	className,
	uniColor,
}: {
	className?: string;
	uniColor?: boolean;
}) => {
	return <h5 className="font-bold">LJA</h5>;
};

export const LogoIcon = ({
	className,
	uniColor,
}: {
	className?: string;
	uniColor?: boolean;
}) => {
	return <h5 className="font-bold">LJA</h5>;
};

export const LogoStroke = ({ className }: { className?: string }) => {
	return <h5 className="font-bold">LJA</h5>;
};
